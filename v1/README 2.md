# Meg - Megalodon

Meg为web项目，egg+vue+webpack+ssr

# dev

``` yarn ```

``` npm run debug ```


# test

``` npm run build ```

# prod

``` npm run build```

``` npm start ```

## 利用npm增加node内存
node --max_old_space_size=10240 /usr/local/bin/npm